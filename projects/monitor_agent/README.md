# Monitor Agent — System Health Checker

**Use Case:** Proactive infrastructure monitoring for client demos  
**Metrics:** CPU, memory, disk, uptime, alert thresholds

## Usage
```bash
python -m projects.monitor_agent.src.agent --simulate
python -m projects.monitor_agent.src.agent
```
