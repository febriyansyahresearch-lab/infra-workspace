[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "infra-workspace"
version = "1.0.0"
description = "Infrastructure toolkit for presales demos, POCs, and deployments"
requires-python = ">=3.10"
